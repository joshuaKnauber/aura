{
  "name": "aura",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter ./packages/core build && pnpm --filter ./packages/react build"
  },
  "devDependencies": {
  }
}
